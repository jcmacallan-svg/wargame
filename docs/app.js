
const STORAGE_KEY = 'owge_v9_state';
const defaultState = {
  scenario: {
    name: "Northern Corridor Crisis",
    overview: "A fictional regional crisis is unfolding around a contested maritime corridor.",
    level: "operational",
    conflict_mode: "grey_zone",
    domains: ["maritime","cyber","information","logistics"],
    initial_blue_situation: "Blue naval forces are protecting territorial waters and a commercial transit corridor while authorities assess spoofing and cyber disruption.",
    turn: 1,
    escalation_stage: 1,
    current_time_label: "H+0",
    current_situation: "Blue naval forces are protecting territorial waters and a commercial transit corridor while authorities assess spoofing and cyber disruption."
  },
  session: {
    session_name: "Exercise Session",
    cells: [
      {"id":"blue-maritime","name":"Blue Maritime","domain":"maritime","role_type":"blue"},
      {"id":"blue-cyber","name":"Blue Cyber","domain":"cyber","role_type":"blue"}
    ]
  },
  cellActions: {},
  latestIntentByCell: {},
  suggestedInjects: [],
  releasedInjects: [],
  redResponse: "",
  playerFeeds: {},
  timeline: []
};
let state = loadState();
let injectLibrary = [];
function deepClone(obj){ return JSON.parse(JSON.stringify(obj)); }
function loadState(){ const raw=localStorage.getItem(STORAGE_KEY); if(!raw) return deepClone(defaultState); try { return JSON.parse(raw); } catch { return deepClone(defaultState); } }
function saveState(){ localStorage.setItem(STORAGE_KEY, JSON.stringify(state)); }
function resetState(){ state=deepClone(defaultState); saveState(); renderAll(); }
window.addEventListener('storage', e => { if(e.key===STORAGE_KEY){ state=loadState(); renderAll(); } });
function getCellFromQuery(){ const p=new URLSearchParams(window.location.search); return p.get('cell'); }
function ensurePlayerFeeds(){ state.session.cells.forEach(c=>{ if(!state.playerFeeds[c.id]) state.playerFeeds[c.id]=[]; if(!state.cellActions[c.id]) state.cellActions[c.id]=[]; }); }
async function init(){ const resp=await fetch('./data/injects.json'); injectLibrary=await resp.json(); ensurePlayerFeeds(); bindEvents(); renderAll(); }
function bindEvents(){ const add=document.getElementById('addCellBtn'); if(add){ add.addEventListener('click',()=>addCellRow()); document.getElementById('saveSetupBtn').addEventListener('click',saveSetup); document.getElementById('generateRedBtn').addEventListener('click',generateRedResponse); document.getElementById('releaseSelectedBtn').addEventListener('click',releaseSelectedInjects); document.getElementById('nextTurnBtn').addEventListener('click',nextTurn); document.getElementById('resetBtn').addEventListener('click',resetState);} const ps=document.getElementById('playerSubmitBtn'); if(ps) ps.addEventListener('click',playerSubmitBlueAction); const sel=document.getElementById('playerCellSelect'); if(sel) sel.addEventListener('change',renderPlayerPage); }
function detectDomain(text){ const t=(text||"").toLowerCase(); const rules=[["maritime",["ship","ships","vessel","fleet","sea","maritime","port","harbor","harbour","coast","territorial waters","shipping","chokepoint","boarding","convoy","patrol boat"]],["air",["air","aircraft","sortie","awacs","radar","intercept","air defense","cap","scramble","flight","isr"]],["land",["land","brigade","battalion","checkpoint","infantry","artillery","border","company","reserve","ground force"]],["cyber",["cyber","network","server","malware","phishing","credentials","intrusion","patch","soc","system","systems"]],["space",["space","satellite","gps","orbital","telemetry","signal"]],["information",["media","narrative","message","messaging","disinformation","deepfake","press","social media","online clip"]],["logistics",["fuel","resupply","logistics","warehouse","transport","maintenance","inventory","pipeline","contractor"]]]; for(const [d,terms] of rules){ if(terms.some(term=>t.includes(term))) return d; } return state.scenario.domains[0]||"maritime"; }
function parseIntent(text, fallbackDomain=null){ const t=(text||"").toLowerCase(); const primary_domain=detectDomain(text)||fallbackDomain||"maritime"; let action_type="general_action", objective="situation_management", tempo="immediate", escalation_signal="low"; const triggers=[]; const rules=[["force_posture_increase",["increase patrol","patrol density","more ships","additional ships","visible patrols","increase presence","deploy additional"]],["territorial_protection",["protect territorial waters","secure waters","protect shipping","secure shipping lanes","protect corridor","deter"]],["containment",["isolate network","contain malware","disconnect system","manual fallback"]],["strategic_communications",["public messaging","counter disinformation","press statement","public statement"]],["surveillance",["monitor","isr","surveillance","observe","track"]],["force_repositioning",["reposition","move reserve","redeploy","shift forces"]]]; for(const [label,phrases] of rules){ if(phrases.some(p=>t.includes(p))) triggers.push(label); } if(triggers.includes("force_posture_increase")) action_type="force_posture_increase"; if(triggers.includes("containment")) action_type="containment"; if(triggers.includes("strategic_communications")) action_type="strategic_communications"; if(triggers.includes("surveillance")) action_type="surveillance"; if(triggers.includes("force_repositioning")) action_type="force_repositioning"; if(triggers.includes("territorial_protection")) objective="territorial_protection"; if(triggers.includes("containment")) objective="service_continuity"; if(triggers.includes("strategic_communications")) objective="narrative_control"; if(triggers.includes("surveillance")) objective="situational_awareness"; if(t.includes("immediately")||t.includes("urgent")) tempo="urgent"; if(t.includes("increase")||t.includes("deploy")||t.includes("launch")||t.includes("board")) escalation_signal="moderate"; if(t.includes("intercept")||t.includes("seize")||t.includes("board")) escalation_signal="high"; return {primary_domain, secondary_domain: primary_domain==="maritime"?"information":null, action_type, objective, tempo, escalation_signal, confidence: triggers.length?0.82:0.58, trigger_phrases: triggers}; }
function addCellRow(cell=null){ const container=document.getElementById('cellsEditor'); if(!container) return; const c=cell||{id:"",name:"",domain:"maritime",role_type:"blue"}; const row=document.createElement('div'); row.className='card cell-row'; row.innerHTML=`<div class="grid3"><div><label>Cell name</label><input type="text" class="cell-name" value="${c.name}"></div><div><label>Cell domain</label><select class="cell-domain">${["maritime","air","land","cyber","information","logistics","space"].map(d=>`<option value="${d}" ${c.domain===d?'selected':''}>${d}</option>`).join('')}</select></div><div><label>Role type</label><select class="cell-role"><option value="blue" ${c.role_type==='blue'?'selected':''}>blue</option></select></div></div><button class="secondary remove-cell-btn" type="button">Remove Cell</button>`; container.appendChild(row); row.querySelector('.remove-cell-btn').addEventListener('click',()=>row.remove()); }
function slugify(s){ return (s||"").toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,''); }
function saveSetup(){ const rows=Array.from(document.querySelectorAll('.cell-row')); const cells=rows.map((row,idx)=>{ const name=row.querySelector('.cell-name').value.trim()||`Blue Cell ${idx+1}`; const domain=row.querySelector('.cell-domain').value; const role_type=row.querySelector('.cell-role').value; return {id: slugify(name)||`blue-cell-${idx+1}`, name, domain, role_type}; }); state.session.session_name=document.getElementById('sessionName').value.trim()||"Exercise Session"; state.session.cells=cells; ensurePlayerFeeds(); saveState(); renderAll(); }
function playerSubmitBlueAction(){ const cellId=getActivePlayerCellId(); const txt=document.getElementById('playerBlueAction').value.trim(); if(!cellId||!txt) return; const cell=state.session.cells.find(c=>c.id===cellId); const intent=parseIntent(txt, cell?cell.domain:null); state.cellActions[cellId]=state.cellActions[cellId]||[]; state.cellActions[cellId].push({turn:state.scenario.turn,time:state.scenario.current_time_label,text:txt,submitted_by:cellId}); state.latestIntentByCell[cellId]=intent; saveState(); document.getElementById('playerBlueAction').value=''; renderAll(); }
function aggregateBlueActionSummary(){ const entries=state.session.cells.map(c=>{ const actions=state.cellActions[c.id]||[]; const last=actions.length?actions[actions.length-1]:null; if(!last||last.turn!==state.scenario.turn) return null; return `${c.name} acted in the ${c.domain} domain.`; }).filter(Boolean); return entries.length?entries.join(' '):'No Blue cell actions submitted this turn.'; }
function collectDomainsThisTurn(){ const domains=new Set(); state.session.cells.forEach(c=>{ const actions=state.cellActions[c.id]||[]; const last=actions.length?actions[actions.length-1]:null; if(last&&last.turn===state.scenario.turn){ const intent=state.latestIntentByCell[c.id]; domains.add((intent&&intent.primary_domain)||c.domain); } }); return Array.from(domains); }
function severityScore(sev){ return ({Low:1,Medium:2,High:3,Strategic:4})[sev]||99; }
function generateRedResponse(){ const domains=collectDomainsThisTurn(); const effectiveDomains=domains.length?domains:[state.scenario.domains[0]]; let matches=injectLibrary.filter(i=>effectiveDomains.includes(i.domain)&&i.level===state.scenario.level&&(i.conflict_mode===state.scenario.conflict_mode||i.conflict_mode==='grey_zone')); matches.sort((a,b)=>severityScore(a.severity)-severityScore(b.severity)); state.suggestedInjects=matches.slice(0,6).map(i=>i.id); state.redResponse=`Red assesses Blue activity across the following domains: ${effectiveDomains.join(', ')}. Red applies calibrated pressure to exploit ambiguity, create friction, and challenge Blue coordination.`; saveState(); renderAll(); }
function releaseSelectedInjects(){ const checkedInjects=Array.from(document.querySelectorAll('input.inject-pick:checked')).map(x=>x.value); const selectedCells=Array.from(document.querySelectorAll('input.cell-pick:checked')).map(x=>x.value); checkedInjects.forEach(id=>{ const inj=injectLibrary.find(x=>x.id===id); if(inj&&!state.releasedInjects.includes(id)) state.releasedInjects.push(id); const targets=selectedCells.length?selectedCells:state.session.cells.map(c=>c.id); targets.forEach(cellId=>{ state.playerFeeds[cellId]=state.playerFeeds[cellId]||[]; state.playerFeeds[cellId].push({turn:state.scenario.turn,time:state.scenario.current_time_label,inject_id:inj.id,text:inj.situation}); }); }); saveState(); renderAll(); }
function advanceTime(turn){ if(turn<=3) return `H+${turn}`; if(turn<=7) return `Day ${turn-2}`; return `Week ${turn-6}`; }
function nextTurn(){ const injectTexts=state.releasedInjects.map(id=>{ const inj=injectLibrary.find(x=>x.id===id); return inj?inj.situation:null; }).filter(Boolean); const summary=`${aggregateBlueActionSummary()} Over the next period, ${injectTexts.length?injectTexts.join(' Meanwhile, '):'no additional incidents were released.'} Meanwhile, Red maintained pressure consistent with the current scenario. The operating picture evolved and requires renewed assessment.`; state.timeline.push({turn:state.scenario.turn,time:state.scenario.current_time_label,text:summary}); state.scenario.current_situation=summary; state.scenario.turn+=1; if(state.scenario.turn%3===0) state.scenario.escalation_stage+=1; state.scenario.current_time_label=advanceTime(state.scenario.turn); state.suggestedInjects=[]; state.releasedInjects=[]; state.redResponse=""; saveState(); renderAll(); }
function renderScenario(){ const el=document.getElementById('scenarioPanel'); if(!el) return; const s=state.scenario; el.innerHTML=`<div><strong>${s.name}</strong></div><div class="small">${s.overview}</div><div class="row" style="margin-top:10px"><span class="tag">Turn: ${s.turn}</span><span class="tag">Time: ${s.current_time_label}</span><span class="tag">Escalation: ${s.escalation_stage}</span><span class="tag">Level: ${s.level}</span><span class="tag">Conflict: ${s.conflict_mode}</span></div><p><strong>Current situation</strong><br>${s.current_situation}</p>`; }
function renderSetup(){ const input=document.getElementById('sessionName'); const editor=document.getElementById('cellsEditor'); const links=document.getElementById('playerLinks'); if(!input||!editor||!links) return; input.value=state.session.session_name||"Exercise Session"; editor.innerHTML=''; state.session.cells.forEach(c=>addCellRow(c)); const basePlayer=`${window.location.origin}${window.location.pathname.replace('index.html','player.html')}`; links.innerHTML=state.session.cells.map(c=>`<div class="linkbox"><strong>${c.name}</strong><br>${basePlayer}?cell=${encodeURIComponent(c.id)}</div>`).join(''); }
function renderCellActions(){ const el=document.getElementById('cellActionsPanel'); if(!el) return; el.innerHTML=state.session.cells.map(c=>{ const actions=state.cellActions[c.id]||[]; const last=actions.length?actions[actions.length-1]:null; const intent=state.latestIntentByCell[c.id]; return `<div class="card"><div><strong>${c.name}</strong></div><div class="row"><span class="tag">${c.domain}</span><span class="tag">${c.id}</span></div><div>${last?last.text:'<span class="small">No action submitted yet.</span>'}</div>${intent?`<div class="small" style="margin-top:8px">Intent: ${intent.action_type} · ${intent.objective} · ${intent.primary_domain}</div>`:''}</div>`; }).join('')||'<div class="small">No cells configured.</div>'; const agg=document.getElementById('aggregateBlueSummary'); if(agg) agg.textContent=aggregateBlueActionSummary(); }
function renderSuggestedInjects(){ const el=document.getElementById('suggestedInjects'); const audience=document.getElementById('injectAudience'); if(!el||!audience) return; if(!state.suggestedInjects.length){ el.innerHTML='<div class="small">No inject suggestions yet.</div>'; } else { el.innerHTML=state.suggestedInjects.map(id=>{ const inj=injectLibrary.find(x=>x.id===id); if(!inj) return ''; return `<label class="card"><input class="inject-pick" type="checkbox" value="${inj.id}"><div><strong>${inj.id}</strong> · ${inj.title}</div><div class="row"><span class="tag">${inj.domain}</span><span class="tag">${inj.severity}</span></div><div>${inj.situation}</div><div class="small">Decision required: ${inj.decision_required}</div></label>`; }).join(''); } audience.innerHTML=`<div class="small">Select audience (leave empty = all cells)</div>${state.session.cells.map(c=>`<label class="tag"><input class="cell-pick" type="checkbox" value="${c.id}"> ${c.name}</label>`).join(' ')}`; }
function renderRedResponse(){ const el=document.getElementById('redResponse'); if(el) el.textContent=state.redResponse||'No Red response generated yet.'; }
function renderTimeline(){ const el=document.getElementById('timeline'); if(!el) return; if(!state.timeline.length){ el.innerHTML='<div class="small">No timeline entries yet.</div>'; return; } el.innerHTML=state.timeline.map(t=>`<div class="card"><div><strong>Turn ${t.turn}</strong> · ${t.time}</div><div>${t.text}</div></div>`).join(''); }
function getActivePlayerCellId(){ const queryCell=getCellFromQuery(); if(queryCell) return queryCell; const sel=document.getElementById('playerCellSelect'); return sel?sel.value:null; }
function renderPlayerPage(){ const cellSelect=document.getElementById('playerCellSelect'); const scenario=document.getElementById('playerScenario'); const feed=document.getElementById('playerFeed'); const playerMeta=document.getElementById('playerMeta'); if(!scenario) return; if(cellSelect){ const current=getCellFromQuery()||cellSelect.value||(state.session.cells[0]&&state.session.cells[0].id); cellSelect.innerHTML=state.session.cells.map(c=>`<option value="${c.id}" ${c.id===current?'selected':''}>${c.name}</option>`).join(''); } const cellId=getActivePlayerCellId()||(state.session.cells[0]&&state.session.cells[0].id); const cell=state.session.cells.find(c=>c.id===cellId); if(playerMeta){ playerMeta.innerHTML=cell?`<span class="tag">${cell.name}</span><span class="tag">${cell.domain}</span>`:'<span class="small">No active cell selected.</span>'; } const s=state.scenario; scenario.innerHTML=`<div><strong>${s.name}</strong></div><div class="small">${s.overview}</div><div class="row" style="margin-top:10px"><span class="tag">Turn: ${s.turn}</span><span class="tag">Time: ${s.current_time_label}</span></div><p><strong>Current situation</strong><br>${s.current_situation}</p>`; const items=(state.playerFeeds[cellId]||[]); if(!items.length){ feed.innerHTML='<div class="small">No player-visible injects released yet for this cell.</div>'; } else { feed.innerHTML=items.map(item=>`<div class="card"><div><strong>Turn ${item.turn}</strong> · ${item.time}</div><div>${item.text}</div></div>`).join(''); } }
function renderAll(){ ensurePlayerFeeds(); renderScenario(); renderSetup(); renderCellActions(); renderSuggestedInjects(); renderRedResponse(); renderTimeline(); renderPlayerPage(); }
init();
